# Core web framework + ASGI server
fastapi==0.115.12
uvicorn[standard]==0.34.0

# Gradio UI (mounted inside FastAPI)
gradio==5.50.0
gradio-client==1.14.0

# HF Hub (must be compatible with gradio 5.50)
huggingface-hub==0.34.6

# Data validation
pydantic==2.12.3
